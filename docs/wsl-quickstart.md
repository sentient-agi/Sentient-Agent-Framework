# Windows / WSL Quickstart

This guide helps Windows users set up a Linux-like environment with **WSL** to run and contribute to this repository.

## 1) Install WSL + Ubuntu
Open **PowerShell as Administrator** and run:
```powershell
wsl --install -d Ubuntu
